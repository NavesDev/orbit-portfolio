{
  "name": "@portfolio/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "migrate": "node --env-file-if-exists=../../.env --experimental-strip-types src/migrate.ts",
    "seed": "node --env-file-if-exists=../../.env --experimental-strip-types src/seed/run.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@portfolio/core": "workspace:*",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/pg": "^8.21.0"
  }
}
