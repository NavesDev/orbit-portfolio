/**
 * Public surface of `@portfolio/telemetry`.
 *
 * `package.json` points `main` and `types` here. The package is deliberately
 * self-contained and is not yet a dependency of `apps/web` — access counting is
 * deferred to Phase 6, so this barrel stays empty until then.
 */

export {};
