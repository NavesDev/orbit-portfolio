/** Between a period's two halves — the prototype's own em dash, spaced. */
export const RANGE_SEPARATOR = ' — ';

/** `YYYY-MM-DD` — the timeline shows years, so the first four characters. */
export const YEAR_LENGTH = 4;
